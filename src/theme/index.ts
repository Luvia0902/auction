// src/theme/index.ts — Design System Tokens（依架構規劃書）
export const Colors = {
    // ── 主色調 ──────────────────────────────────────
    bg: '#0A1628',   // 深海藍黑（背景）
    surface: '#1E293B',   // 暗石板（卡片）
    surfaceHigh: '#293548',   // 卡片 hover
    border: '#334155',   // 邊框
    primary: '#3D7EFF',   // 電光藍（按鈕/連結）
    accent: '#F59E0B',   // 琥珀金（CTA/價格）

    // ── 亮色主題 (Light Mode) ───────────────────────
    bgLight: '#F5F5FA',    // 淺灰背底
    cardLight: '#FFFFFF',  // 白色卡片
    brandBlue: '#13337A',  // 頂部導覽列深藍色
    borderLight: '#E2E8F0',// 亮色邊框
    iconBg: '#E8EDF6',     // Icon 圓形按鈕底色

    // ── 文字 ────────────────────────────────────────
    textPrimary: '#F1F5F9',
    textSecondary: '#94A3B8',
    textMuted: '#64748B',
    textInverse: '#0A1628',
    textDarkPrimary: '#1E293B', // 亮色背景用的深色字
    textDarkSecondary: '#475569',
    textDarkMuted: '#94A3B8',

    // ── 拍次色彩 ────────────────────────────────────
    round1: '#FBBF24',   // 一拍：金
    round2: '#94A3B8',   // 二拍：銀
    round3: '#EF4444',   // 三拍+：紅（折扣大）

    // ── 點交狀態 ────────────────────────────────────
    delivery: '#10B981',  // 點交：翠綠
    noDelivery: '#F97316',  // 不點交：橙

    // ── 開標結果 ────────────────────────────────────
    sold: '#22C55E',  // 得標
    unsold: '#64748B',  // 流標
    cancelled: '#EF4444',  // 停拍

    // ── 風險燈號 ────────────────────────────────────
    riskHigh: '#DC2626',  // 🔴 高
    riskMedium: '#F59E0B',  // 🟡 中
    riskLow: '#16A34A',  // 🟢 低

    // ── AI 品牌 ─────────────────────────────────────
    ai: '#818CF8',  // 柔紫（AI 功能專屬色）
} as const;

export const Typography = {
    // 字重
    regular: '400' as const,
    medium: '500' as const,
    semibold: '600' as const,
    bold: '700' as const,
    // 字級
    xs: 11,
    sm: 13,
    base: 15,
    md: 16,
    lg: 18,
    xl: 22,
    xxl: 28,
    xxxl: 34,
} as const;

export const Spacing = {
    xxs: 2,
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
    xxl: 32,
    xxxl: 48,
} as const;

export const Radius = {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 20,
    pill: 999,
} as const;

export const Shadow = {
    card: {
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.3,
        shadowRadius: 8,
        elevation: 4,
    },
    glow: {
        shadowColor: '#3D7EFF',
        shadowOffset: { width: 0, height: 0 },
        shadowOpacity: 0.4,
        shadowRadius: 12,
        elevation: 6,
    },
} as const;
